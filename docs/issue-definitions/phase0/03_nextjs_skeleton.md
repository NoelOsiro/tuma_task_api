# Next.js skeleton + Auth0 integration (task)

Description

- Create Next.js app skeleton with `@auth0/nextjs-auth0` configured for dev tenant
- Setup Tailwind CSS and base layout

Acceptance criteria

- Dev server runs and Auth0 login redirects work
- Basic pages: /, /login, /dashboard (protected)
