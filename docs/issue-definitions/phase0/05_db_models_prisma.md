# Basic Postgres models + Prisma schema/migrations (task)

Description

- Translate the data model in `docs/04-data-model.md` to a `prisma/schema.prisma` file
- Prepare initial `prisma` setup and a first migration

Acceptance criteria

- `prisma/schema.prisma` exists under `/prisma`
- Developer instructions in README to run `prisma migrate dev` against dev DB
