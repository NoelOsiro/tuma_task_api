# Dev Postgres + Redis + object storage (task)

Description

- Provision a dev Postgres instance (local Docker or managed)
- Provision a dev Redis instance (local Docker or managed)
- Provision S3-compatible object storage (Minio, Supabase, or S3)

Acceptance criteria

- A `.env.example` contains dev connection strings (placeholders)
- Services are reachable from local environment or docker-compose
