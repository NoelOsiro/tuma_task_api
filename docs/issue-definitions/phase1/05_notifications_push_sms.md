# Basic notifications (push + SMS fallback) (task)

Description

- Integrate FCM for push notifications
- Add SMS fallback via Twilio or Africa's Talking for critical flows

Acceptance

- Workers receive test push; fallback SMS can be sent via a test endpoint
