# Phase 0 — Foundation (Epic)

This epic groups foundation tasks required before MVP development.

Planned child tasks are in `docs/issue-definitions/phase0/` and will be created as issues labeled `task`.

Child tasks:

- Auth0 tenant + apps configured (dev/prod)
- Dev Postgres + Redis + object storage
- Next.js skeleton + Auth0 integration
- FastAPI skeleton + JWT verify
- Basic Postgres models + Prisma schema/migrations
