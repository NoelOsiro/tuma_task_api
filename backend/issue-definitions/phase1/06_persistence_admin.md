# Postgres persistence, admin basic views (task)

Description

- Ensure core tables are persisted in Postgres
- Create simple admin endpoints or pages for listing users, tasks, and disputes

Acceptance

- Admin can view users and tasks via protected endpoints
