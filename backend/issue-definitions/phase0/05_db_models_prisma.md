
# Basic Postgres models + Prisma schema/migrations (task)

Description

- Define core Postgres schema for users, tasks, assignments, payments
- Create Prisma schema and initial migration

Acceptance

- Prisma client can perform basic CRUD and migrations apply locally
