# Example environment variables for local development
# Copy this to `.env` and replace the placeholders with real values.

# Auth0
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_AUDIENCE=https://tuma.example.com/api
AUTH0_ISSUER=https://your-tenant.us.auth0.com/

# Client IDs / secrets (dev and prod)
# Fill these with the correct client IDs for your Auth0 Applications.
AUTH0_DEV_SPA_CLIENT_ID=your-dev-spa-client-id
AUTH0_DEV_SPA_CLIENT_SECRET=your-dev-spa-client-secret
AUTH0_PROD_SPA_CLIENT_ID=your-prod-spa-client-id
AUTH0_PROD_SPA_CLIENT_SECRET=your-prod-spa-client-secret

# Database (Postgres)
DATABASE_URL=postgresql://user:password@localhost:5432/tuma_dev

# Redis (optional)
REDIS_URL=redis://localhost:6379/0

# Other
PROJECT_NAME=TumaTask Backend


# Postgres
POSTGRES_USER=tuma
POSTGRES_PASSWORD=tuma_pass
POSTGRES_DB=tuma_dev
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# App
APP_ENV=development
APP_DEBUG=true
